# AMS-SemiCD dependencies
# Verified with Python 3.10.19 + PyTorch 2.6.0 (cu124) on a single NVIDIA GPU.
# Install the CUDA build of PyTorch that matches your local driver first, e.g.:
#   pip install torch==2.6.0+cu124 torchvision==0.21.0+cu124 --index-url https://download.pytorch.org/whl/cu124

# ---- core (required for training / testing) ----
torch==2.6.0
torchvision==0.21.0
triton==3.2.0
numpy==2.2.6
pillow==12.0.0
opencv-python==5.0.0.93
einops==0.8.1
timm==0.4.12
fvcore==0.1.5.post20221221
matplotlib==3.10.8
tqdm==4.67.1
tensorboardX==2.6.4
ptflops==0.7.5
torchstat==0.0.7

# ---- CUDA extension build toolchain (for the selective-scan kernel) ----
# selective-scan==0.0.2 is NOT on PyPI: build it from the vendored source with
#   cd model/vmamba/kernels/selective_scan && python setup.py install
ninja==1.13.0
packaging==25.0
setuptools==80.9.0
wheel==0.45.1

# ---- optional (log inspection / result analysis / unit tests) ----
# tensorboard==2.20.0
# pandas==2.3.3
# seaborn==0.13.2
